# PROMPT 00 — 기존 코드 AUDIT (가장 먼저 실행)
> 다른 프롬프트 실행 전 반드시 이것부터 실행하세요.
> 기존 yusay 프로젝트 현황을 파악하고 통합 전략을 수립합니다.

---

당신은 기존 React Native 프로젝트를 분석하고 새 스펙과 통합하는 전문가입니다.

## 중요 원칙
- 기존 코드를 덮어쓰거나 삭제하지 마세요
- DB 테이블 DROP은 절대 금지, ALTER TABLE ADD COLUMN만 허용
- 기존 데이터는 반드시 보존

## 분석 순서

### 1단계 — 프로젝트 구조 파악
```
기존 yusay 폴더의 전체 디렉토리 트리를 출력하고:
- 현재 기술 스택 (package.json 분석)
- Expo SDK 버전
- 라우팅 방식 (Expo Router / React Navigation)
- 상태관리 라이브러리
- UI 라이브러리
- 백엔드 연결 방식
```

### 2단계 — 구현된 화면 목록
```
app/ 폴더의 모든 라우트 파일을 나열하고:
- 각 화면의 구현 완성도 (완성 / 미완성 / 스켈레톤)
- 현재 동작하는 기능 목록
- 현재 동작하지 않는 기능 목록
```

### 3단계 — DB 스키마 현황
```
supabase/ 또는 DB 관련 폴더를 분석하고:
- 현재 존재하는 테이블 목록과 컬럼
- 기존 RLS 정책
- 기존 Edge Functions
- 기존 인덱스
```

### 4단계 — GAP 분석 (YUSAY_MASTER.md 기준)

아래 형식으로 출력:

```
=== GAP 분석 보고서 ===

✅ 구현됨 (건드리지 않아도 됨):
- [항목명]: [현재 상태]
...

⚠️ 수정 필요 (기존 파일 부분 수정):
- [항목명]: [현재 vs 목표]
...

❌ 미구현 (새로 만들어야 함):
- [항목명]: [참조 프롬프트 번호]
...

🗄️ DB 마이그레이션 필요:
- ALTER TABLE 필요: [테이블명] ADD COLUMN [컬럼명] [타입]
- CREATE TABLE 필요: [테이블명]
- RLS 추가 필요: [정책명]
...
```

### 5단계 — 통합 실행 계획 제안

GAP 분석 결과를 바탕으로:
1. 리스크 없이 먼저 할 수 있는 작업
2. 기존 코드 수정이 필요한 작업 (충돌 가능성 있음)
3. 마지막에 해야 할 작업 (전체 구조 변경)

순서를 추천해주세요.

---

분석 완료 후, 제가 "시작해" 라고 하면
위 실행 계획에 따라 하나씩 진행합니다.
한 번에 모든 프롬프트를 실행하지 말고,
각 단계 완료 후 제게 확인을 받으세요.
