// Re-export from canonical location for backward compatibility
export { isFeatureAllowed, getGateType, getUpgradeTarget, getMonthlyLimit } from '../../constants/featureGates';
export type { FeatureKey, PlanType, GateType } from '../../constants/featureGates';
