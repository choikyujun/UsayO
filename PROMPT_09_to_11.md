# PROMPT 09 — 캘린더 4개 뷰 (월간·주간·일간·연간)
> Claude Code에게 전달하는 YuSay 캘린더 화면 구현 프롬프트

---

당신은 React Native 캘린더 UI 전문가입니다.
YuSay 캘린더 탭의 4가지 뷰를 구현해주세요.

## 공통 구조

```tsx
// app/(tabs)/calendar.tsx
// 상단: 월/연도 헤더 + 좌우 화살표
// 뷰 전환 탭: [일 | 주 | 월 | 연]
// 각 뷰 컴포넌트 교체
// FAB 항상 표시 (BottomNav)
```

## 1. 월간 뷰 (MonthlyView)

```
7열 그리드 (일~토)
각 날짜 셀:
  - 날짜 숫자 (오늘: 보라 원, 선택: 연보라 원)
  - 도트 인디케이터 (최대 3개)
    - 업무: #534AB7
    - 개인: #1D9E75
    - 중요: #D85A30
  - 도트 3개 초과: 말줄임 처리

날짜 탭 → 바텀시트 슬라이드업:
  - 달력 그대로 유지 (전체화면 전환 아님)
  - 해당 일 일정 리스트
  - 일정 없으면 "일정 없는 날" + 음성 등록 유도
```

## 2. 주간 뷰 (WeeklyView)

```
세로 타임라인 (시간축 9AM~9PM)
가로 7열 (일~토)
현재 시각 빨간 가로선 (실시간 업데이트, 1분 인터벌)
이벤트 블록:
  - 절대 위치: top = (시작시각 - 9AM) / 12h * 100%
  - 높이: 소요시간 / 12h * 100%
  - 배경: 카테고리 컬러 20% opacity
  - 좌측 테두리: 카테고리 컬러 2.5px solid
오늘 컬럼: 배경 rgba(83,74,183,0.04) 미세 강조
빈 시간 (>2시간): 점선 박스 "여유 Nh" 표시
겹치는 일정: 가로로 분할 (각 50% 너비)
```

## 3. 일간 뷰 (DailyView)

```
날짜 헤더:
  - 날짜 큰 텍스트
  - 요약 칩 3개: "N개 일정" / "N시간 여유" / 밀집도

시간 타임라인:
  - 좌측 시간 레이블 (0~23시)
  - 우측 이벤트 영역
  - 이벤트 블록 (높이 = 소요시간)
  - 빈 슬롯 점선 표시 (>30분)
  - 현재 시각 빨간 라인 + 시각 텍스트

이벤트 블록 탭 → 상세 모달 또는 상세 화면
```

## 4. 연간 뷰 (YearlyView)

```
3열 4행 그리드 (12개월)
각 달 미니 달력:
  - 월 이름
  - 7x5 미니 날짜 그리드
  - 각 날짜: 히트맵 색상 (0~4 강도)
    - h0: #13112A (일정 없음)
    - h1: #2A2460 (1개)
    - h2: #3C3489 (2개)
    - h3: #534AB7 (3개)
    - h4: #7F77DD (4개+)
  - 오늘: 보라 원 강조
  - 현재 달: 배경 미세 강조

달 탭 → 월간 뷰로 전환 (해당 달로 이동)
하단 범례: "적음 ○ ○ ○ ○ ○ 많음"

바쁜 달 자동 감지:
  - 가장 밀집한 달에 "🔥" 표시
  - 이 달 일정 개수 툴팁
```

## 뷰 전환 애니메이션
- 탭 전환: FadeIn 200ms
- 월/주 이동: SlideX 애니메이션
- Reanimated v3 사용

## 데이터 로딩
- 현재 보이는 날짜 범위만 쿼리 (성능)
- 무한 스크롤: 앞뒤로 드래그 시 자동 로드
- 로딩 중: 스켈레톤 UI

---

# PROMPT 10 — 다가올 탭 + 설정 탭
> Claude Code에게 전달하는 YuSay 다가올·설정 화면 프롬프트

---

당신은 React Native UI 전문가입니다.
다가올 탭과 설정 탭을 구현해주세요.

## 다가올 탭 (UpcomingScreen)

### 이번 주 뷰
```tsx
// 탭 전환: [이번 주 | 이번 달]
// 요일별 그룹핑
// DayGroup 컴포넌트:
//   - 요일 헤더: "오늘 · 월 5/26" (오늘 초록), "내일 · 화 5/27"
//   - EventItem 리스트 (시간 + 컬러바 + 제목 + 장소)
//   - 일정 없는 날: "일정 없음 · 여유로운 하루" (초록 텍스트)
//   - 다음 일정 뱃지: "곧" 뱃지

// 최상단: D-Day 뱃지 (오늘 다음 중요 일정)
```

### 이번 달 뷰
```tsx
// 주차별 그룹 (1주차, 2주차...)
// 각 주: 헤더 + 퍼플 그라디언트 언더라인
// 일정 항목 + 탭하면 해당 날로 이동
// D-Day 카운트: "D-8", "D-3" 뱃지
// AI 인사이트 카드:
//   - "이번 달 가장 여유로운 주는 3주차예요"
//   - 배경: 퍼플→그린 그라디언트
```

### AI 빈 슬롯 탭 (선택)
```tsx
// Pro 이상만 접근 (FeatureGate)
// 비어있는 시간 슬롯 3개 카드
// 각 카드: 날짜·시간·여유 시간 + [🎙 일정 잡기] 버튼
// "일정 잡기" → 음성 플로우 시작 (해당 시간 미리 채움)
// 하단 힌트: "음성으로 '이번 주 빈 시간 찾아줘'라고 말해도 돼요"
```

## 설정 탭 (SettingsScreen)

### 설정 메인
```tsx
// 프로필 섹션:
//   - 아바타 (이니셜 원형)
//   - 이름, 이메일
//   - 플랜 뱃지 (Free/Pro/Team)
//   - 탭 → 프로필 편집

// 업그레이드 배너 (Free 유저만):
//   - "Pro로 업그레이드" 카드
//   - 가격 직접 표시 (₩3,900/월)
//   - "보기 →" 버튼

// 메뉴 리스트:
//   캘린더 연동 → CalendarIntegrationScreen
//   알림 설정 → NotificationSettingsScreen
//   언어·음성 → LanguageSettingsScreen
//   프라이버시 → PrivacySettingsScreen
//   앱 정보 → AppInfoScreen
//   ─────────
//   로그아웃 (빨간 텍스트)
```

### 캘린더 연동 (CalendarIntegrationScreen)
```tsx
// Google Calendar: 연결됨 ✓ (초록) / 연결 안됨
// Apple Calendar: 연결됨 / 연결 안됨
// Naver Calendar: 준비 중 (회색)
// 연결 버튼 → OAuth 플로우 (supabase auth providers)
// 연동 해제 버튼
// 마지막 동기화 시각
```

### 알림 설정 (NotificationSettingsScreen)
```tsx
// [일정 알림]
// 시작 10분 전: 토글 on
// 시작 1시간 전: 토글 on
// 하루 전 요약: 토글 off
// [음성 알림]
// 이어폰 음성 읽기: 토글 on
// 무음 시 진동: 토글 on

// expo-notifications 설정 연동
```

### 언어·음성 (LanguageSettingsScreen)
```tsx
// 인식 언어 목록:
//   🇰🇷 한국어: 선택됨 (체크)
//   🇺🇸 English: Pro 뱃지 (잠금)
//   🇯🇵 日本語: Pro 뱃지 (잠금)
//   🌏 동남아: Team 뱃지 (잠금)
// 잠긴 언어 탭 → UpgradeModal

// TTS 속도 슬라이더 (0.5x ~ 2.0x)
// 테스트 재생 버튼: "내일 오후 3시에 팀 회의 잡았어요"
```

### 프라이버시 (PrivacySettingsScreen)
```tsx
// 녹음 즉시 삭제: 토글 (기본 on, 변경 불가)
// On-Device 처리: Team 플랜만, 토글
// 분석 데이터 수집: 토글 (익명 통계)
// ─────────────────
// 내 데이터 삭제: 빨간 텍스트 → 확인 알럿
//   "모든 일정과 음성 기록을 삭제합니다. 되돌릴 수 없어요."
//   [취소] [삭제하기]
```

---

# PROMPT 11 — 온보딩 7단계
> Claude Code에게 전달하는 YuSay 온보딩 플로우 구현 프롬프트

---

당신은 React Native 온보딩 UX 전문가입니다.
YuSay 최초 설치 시 표시되는 온보딩 플로우를 구현해주세요.

## 온보딩 진입 조건

```typescript
// app/_layout.tsx
const hasCompletedOnboarding = await AsyncStorage.getItem('onboarding_complete');
if (!hasCompletedOnboarding) {
  router.replace('/onboarding/splash');
}
```

## 7단계 화면

### 1. 스플래시 (onboarding/splash.tsx)
```tsx
// 전체 화면 #0E0C1F
// 중앙: YuSay 로고 (퍼플 원 + 파형 아이콘)
// 파형 애니메이션: 5개 막대 높이 변화
// 로고 아래: "YuSay" Space Mono Bold 32px
// 태그라인: "Yu say. It's done." 12px #4a4670
// 하단: 로딩 바 (1.5초 → 자동 다음 화면)
```

### 2~4. 소개 슬라이드 (onboarding/slide-1,2,3.tsx)
```tsx
// 공통 구조:
// - 전체 화면 다크
// - 중앙 일러스트 영역 (SVG 애니메이션)
// - 큰 타이틀 (18px Bold)
// - 설명 텍스트 (12px Regular)
// - 하단: 진행 도트 (현재 = 보라 pill, 나머지 = 작은 원)
// - "다음" 버튼 / 마지막은 "시작하기"
// - 좌우 스와이프 가능

// 슬라이드 1: 말하면 잡혀요
//   일러스트: 마이크 → 화살표 → 캘린더
//   타이틀: "말하면 일정이 잡혀요"
//   설명: '"내일 오후 3시에 팀 회의 잡아줘" → 완료'

// 슬라이드 2: 전부 음성으로
//   일러스트: 📝 생성 / ✏️ 수정 / 🗑️ 삭제 카드 3개
//   타이틀: "전부 음성으로 됩니다"
//   뱃지: "업계 유일 완전 음성 CRUD"

// 슬라이드 3: 한국어 특화
//   일러스트: 🇰🇷 + 말풍선 예시들
//   타이틀: "한국어를 제대로 알아요"
//   예시: "내일 오후" → 14:00 / "퇴근 후" → 18:00
```

### 5. 마이크 권한 (onboarding/permission-mic.tsx)
```tsx
// 마이크 아이콘 (보라 원)
// 타이틀: "마이크를 허용해주세요"
// 설명 카드:
//   ✓ 말하는 순간에만 사용해요
//   ✓ 녹음은 저장되지 않아요
//   ✓ 언제든 권한 해제 가능

// [마이크 허용하기] 버튼 → Audio.requestPermissionsAsync()
// [나중에] 텍스트 버튼 → 다음 화면으로

// 권한 거부 시: 설정 앱으로 이동 안내
```

### 6. 캘린더 연동 (onboarding/calendar-connect.tsx)
```tsx
// 타이틀: "캘린더를 연결해볼까요?"
// 설명: "기존 일정을 바로 가져올 수 있어요"

// Google Calendar 옵션 카드 (라디오 선택)
// Apple Calendar 옵션 카드

// [연결하기] → OAuth 플로우
// [건너뛰기] → 다음 화면 (설정에서 나중에 가능)

// 주의: 강제 아님, 항상 건너뛰기 제공
```

### 7. 준비 완료 (onboarding/ready.tsx)
```tsx
// 배경: 다크→다크그린 그라디언트
// 체크 아이콘 (초록 원, scale-in 애니메이션)
// "준비 완료!" 타이틀 초록
// 연결 상태 확인 (마이크 ✓ / 캘린더 ✓ 또는 미연결)

// "첫 번째 일정 만들어볼까요?" 제안 카드
//   예시: "내일 오후 3시 회의 잡아줘"

// [시작하기 →] 버튼 → 홈 화면 이동 + 온보딩 완료 플래그 저장

// AsyncStorage.setItem('onboarding_complete', 'true')
// router.replace('/(tabs)/')
```

## 온보딩 완료 후 첫 음성 유도

홈 화면 도착 후 2초 뒤:
```typescript
// 예시 말풍선 표시 (3초 후 자동 사라짐)
// "이렇게 말해보세요: '내일 오후 3시에 회의 잡아줘'"
// FAB 버튼 pulse 강조 (첫 탭 유도)
```

Expo Router 사용, 스와이프 제스처는 react-native-gesture-handler.
온보딩 중 앱 종료 후 재진입 시 중단 지점부터 재개.
